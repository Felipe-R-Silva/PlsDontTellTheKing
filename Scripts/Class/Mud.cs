using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mud : MonoBehaviour {
    /*
      //public GameObject mudPrefab;
    private List<GameObject> mudPieces;
    private List<float> mudTimeColected;
    // Use this for initialization

    public Mud(GameObject parent)
    {   
        var children = parent.GetComponentsInChildren<GameObject>();
        //mudPieces = new GameObject[parent.transform.childCount];
        //mudTimeColected = new float[parent.transform.childCount];
        for (int i=0;i< parent.transform.childCount;i++)
        {
            mudPieces.Add(children[i]);
            //mudPieces[i] = children[i];
           // mudTimeColected[i] = -9999;
            mudTimeColected.Add(-9999);
        }
    }

    public void setMudTime(float time,int position)
    {

    }
    public void printMudNames()
    {
        for (int i = 0; i < mudPieces.Count; i++)
        {
            print(mudPieces[i].name);

        }
    }
    public List<GameObject> getMudArr()
    {
        return mudPieces;
    }
    */
}
